[workspace]
resolver = "2"
members = [
    "energy-registry",
    "payout-splitter",
]

[profile.release]
opt-level = "z"
overflow-checks = true
debug = false
panic = "abort"
lto = true
codegen-units = 1
